
###### INSTRUCTIONS TO RUN THE API ############################

Unzip the Ople.zip and move inside folder "Ople

Now, issue following commands to run and test the service:

1. docker-compose build (wait till dependencies are downloaded)

2. docker-compose up

3. run the script ./curl_commands.sh (Run this command in new tab from same location)